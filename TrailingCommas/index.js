// Now there is no issues anymore when using trailing commas (in the past you would get an error);
function add(param1,) {
    const example = {
        name: 'Henrique',
    }

    console.log(example);
}

add(2);